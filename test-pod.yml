apiVersion: v1
kind: Pod
metadata:
  name: test-pod
spec:
  containers:
  - name: nginx
    image: nginx:latest
    resources:
      requests:
        cpu: "100m"  # 0.1 CPU core
        memory: "128Mi" # 128 Mebibytes of memory